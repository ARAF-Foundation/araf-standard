#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run the MDX frontmatter audit before commit
node scripts/audit-mdx-frontmatter.js
RESULT=$?
if [ $RESULT -ne 0 ]; then
  echo "\n[HUSKY] Commit blocked: All .mdx files must have a single YAML frontmatter block at the top."
  exit 1
fi
